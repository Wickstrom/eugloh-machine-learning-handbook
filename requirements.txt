# Mirror of [project] dependencies in pyproject.toml — needed because repo2docker
# (used by Binder) does not always read pyproject.toml. Keep in sync with
# pyproject.toml.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
scipy>=1.10
scikit-learn>=1.3
ipykernel>=6.0
ipywidgets>=8.0
jupyter>=1.0
jupyterlab>=4.0
nbconvert>=7.0
marimo>=0.17
